<!-- if loged in -->
<% if current_user != nil %>
<!-- If admin -->
	<% if current_user.admin %>
		<div id='adminnoticediv'>
			<a class="notice"><%= flash[:notice] if flash[:notice] %></a>
			<a class="alert"><%= flash[:alert] if flash[:alert] %></a>
		</div>
		<div id="adminstreamheader">Admin Video's Controlls</div>
		<div class='hvr-grow lasthvrgrow'>
			<a class="various iframe" data-fancybox-type="iframe" style="text-decoration : none" href="<%= new_video_path %>"><input type="submit" class="guildbutton" value="Add Video" /></a>
		</div>
		<div id="adminstreamerlist">
		    <% @videos.reverse.each do |admin| %>
		        <div id='adminstreamername'><%= admin.text %></div>
		        <a class="various iframe" data-fancybox-type="iframe" style="text-decoration : none" href="<%= edit_video_path(admin) %>">Edit</a>
		        <div id="adminstreamerdelete"><%= link_to 'Delete', admin, method: :delete, data: { confirm: 'Are you sure?' } %></div>
		        <br>
		    <% end %>
		</div>
		<div id="back-button">
			<%= link_to "Back", admin_index_path %>
		</div>

<!-- If not admin -->
	<% else %>
		<%= render 'shared/noaccess' %>	   
	<% end %>
<% else %>
	<%= render 'shared/noaccess' %>	 
<% end %>


re-add videos that i deleted

fix youtube channel thing so latest always first???

text and email if application was submitted....

