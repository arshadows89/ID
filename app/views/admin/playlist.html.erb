<!-- if loged in -->
<% if current_user != nil %>
<!-- If admin -->
  <% if current_user.admin %>


Video
 - Edit video only (no add)
 - need really good regex so people dont fuck up
 - should give good instructions

need database to store the video

Playlist
  link

need validations

    <div id="back-button">
      <%= link_to "Back", admin_home_path %>
    </div>
<!-- If not admin -->
  <% else %>
    <%= render 'shared/noaccess' %>    
  <% end %>
<% else %>
  <%= render 'shared/noaccess' %>  
<% end %>

