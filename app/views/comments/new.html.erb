<!-- if loged in -->
<% if current_user != nil %>
	<%= render 'form' %>

<!-- If admin -->
	<% if current_user.admin %>
<!-- If not admin -->
	<% else %>
		<%= render 'shared/noaccess' %>	   
	<% end %>
<% else %>
	<%= render 'shared/noaccess' %>	 
<% end %>