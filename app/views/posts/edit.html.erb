<!-- if loged in -->
<% if current_user != nil %>
<!-- If admin -->
	<% if current_user == @post.user || current_user.admin %>
		<%= render 'form' %>
	<% else %>
		<%= render 'shared/noaccess' %>	   
	<% end %>
<% else %>
	<%= render 'shared/noaccess' %>	 
<% end %>

